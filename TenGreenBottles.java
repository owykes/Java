public class TenGreenBottles {
    public static void main(String[] arg) {
    int numBottles = 10;
    String word = "bottles";
    
    while (numBottles > 0) {

        if (numBottles == 1) {
            word = "bottle";
        }

        System.out.println(numBottles + " green " + word + ", hanging on the wall");
        System.out.println(numBottles + " green " + word + ", hanging on the wall");
        System.out.println("And if one green bottle should accidently fall");
        numBottles = numBottles - 1;

        if (numBottles > 0) {
            System.out.println("There'll be " + )
        }
    }
    }
}
